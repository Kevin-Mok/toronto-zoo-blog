# Archive Docs

Use this subtree for one-off outputs and historical artifacts that should be kept for traceability but are not reusable templates.

Current layout:

- `og/`: archived OG prompt run outputs.
