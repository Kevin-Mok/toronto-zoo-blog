# Reusable Docs

Use this subtree for reusable prompt and template assets that are not tied to a single date/run.

Current layout:

- `prompts/og/`: reusable Open Graph prompt assets.
- `prompts/blog/`: reusable blog generation/publish prompt assets.
